# ============================================================================
# Production Environment Variables (Vercel / Serverless Deployment)
# ----------------------------------------------------------------------------
# Copy this file to `.env.production` (or add the variables manually in Vercel
# Settings → Environment Variables) and fill in the real secrets.
#
#  NOTE
#  -----
#  1. Variables WITHOUT the `VITE_` prefix are ONLY available to your
#     serverless API routes / edge functions.
#  2. Variables WITH the `VITE_` prefix are also exposed to the browser at
#     build-time – never put secret keys in them.
# ============================================================================

###############################################################################
# AI Provider API Keys – server-only                                           #
###############################################################################
OPENAI_API_KEY=your_openai_api_key_here
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Volcano Engine – Large Language Model (LLM) / Image Gen – server-only
# Note: VOLCENGINE_LLM_API_KEY is used for both LLM and image generation (Doubao API)
VOLCENGINE_LLM_API_KEY=your_volcengine_llm_api_key_here
# Legacy keys (kept for backward compatibility)
VOLCENGINE_VISUAL_API_KEY=your_volcengine_visual_api_key_here
VOLCENGINE_VISUAL_SECRET_KEY=your_volcengine_visual_secret_key_here
# Alternative key name for Doubao API (fallback if VOLCENGINE_LLM_API_KEY not set)
ARK_API_KEY=your_ark_api_key_here

###############################################################################
# AI Provider API Keys – client-exposed (use low-privilege / proxy keys!)      #
###############################################################################
VITE_VOLCENGINE_LLM_API_KEY=public_volcengine_llm_api_key_here
VITE_VOLCENGINE_VISUAL_API_KEY=public_volcengine_visual_api_key_here

###############################################################################
# Supabase (database / auth)                                                   #
###############################################################################
# Public keys for the browser
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=public_anon_key_here

# Service-role key – server-only (NEVER expose to browser)
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_KEY=service_role_key_here

###############################################################################
# Payment / Paywall (Stripe)                                                   #
###############################################################################
# Feature flag – identical value should be used for both variables.
PAYWALL_VERSION=prod       # server-only
VITE_PAYWALL_VERSION=prod  # browser-visible

# Stripe credentials
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key_here       # server-only
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret_here # server-only
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_your_publishable_key_here # browser-visible

# Paywall behaviour tuning
PAYWALL_PERSISTENCE=db   # "db" | "ram" – how to count credits (defaults to ram)

# Optional: override default table names used by serverless functions
CREDITS_TABLE=credits
PURCHASES_TABLE=purchases

###############################################################################
# Email Service (for email verification)                                     #
###############################################################################
# Resend API key for sending verification emails
RESEND_API_KEY=re_MLHsMNci_dmc5NYkzYRj5qQpzUjRNvzTv

###############################################################################
# API Routing                                                                  #
###############################################################################
# Override if your client should hit a different endpoint than `/api/chat`
# VITE_SERVERLESS_API_URL=https://your-app.vercel.app/api/chat

###############################################################################
# Development-only toggles (leave untouched for production)                    #
###############################################################################
VITE_DIRECT_API_MODE=false   # DO NOT set to "true" in production!

# Local-only OpenAI / DeepSeek keys used when VITE_DIRECT_API_MODE=true.
# VITE_OPENAI_API_KEY=
# VITE_DEEPSEEK_API_KEY=
